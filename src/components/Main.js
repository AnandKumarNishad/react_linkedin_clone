import styled from "styled-components";
import React, { Component } from 'react';

const Main = (props) => {
    return (
        <Conatainer>
            main
        </Conatainer>
    );
};

const Conatainer = styled.div`
    display: flex;
`;

export default Main;